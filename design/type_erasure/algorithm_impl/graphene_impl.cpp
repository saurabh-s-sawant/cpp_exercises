#include "materials_impl.H"
#include <iostream>

void computeStep1(Graphene const& graphene) {
    std::cout << "processing step 1 for Graphene with unitcells: " << graphene.get_unitcells() << "\n";
}
